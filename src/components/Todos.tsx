import { ChangeEvent,FormEvent,useState } from "react";
import { v4 as uuidv4 } from "uuid";

interface Props {
  todo:string
}

export const Todos = () => {
  
  return (
    <div>Todos</div>
  )
}

